{ pkgs, config, ... }:
{
  time = "2025-06-27T18:48:11+00:00";
  condition = config.programs.ashell.enable;
  message = ''
    Version 0.5.0 of `programs.ashell` introduces a new configuration file format.
    The camelCase format has been removed in favor of snake_case, which better aligns with the toml syntax.
  '';
}
